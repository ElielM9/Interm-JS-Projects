/* Selectores de elementos del DOM */
const CRYPTO_SELECT = document.querySelector(`#cryptocurrency`);
const CURRENCY_SELECT = document.querySelector(`#currency`);
const QUOTE_FORM = document.querySelector(`#quote-form`);

export { CRYPTO_SELECT, CURRENCY_SELECT, QUOTE_FORM };
